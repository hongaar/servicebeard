apiVersion: v1
kind: Service
metadata:
  name: {{ include "serviceboard.fullname" . }}-api
  labels:
    {{- include "serviceboard.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.api.port }}
      targetPort: {{ .Values.api.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "serviceboard.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: api
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "serviceboard.fullname" . }}-web
  labels:
    {{- include "serviceboard.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.web.port }}
      targetPort: {{ .Values.web.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "serviceboard.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: web
