{
  "name": "servicebeard",
  "private": true,
  "license": "MIT",
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "docker compose up -d && bun run dev:apps",
    "dev:apps": "bun --elide-lines=50 run --filter './apps/*' dev",
    "dev:api": "bun --elide-lines=50 run --filter @servicebeard/api dev",
    "dev:web": "bun --elide-lines=50 run --filter @servicebeard/web dev",
    "dev:worker": "bun --elide-lines=50 run --filter @servicebeard/worker dev",
    "build": "bun run --filter '*' build",
    "build:landing": "bun run --filter @servicebeard/web build:landing",
    "typecheck": "bun run --filter '*' typecheck",
    "lint": "eslint .",
    "test": "bun test tests/",
    "db:generate": "bun run --filter @servicebeard/db generate",
    "db:migrate": "bun run --filter @servicebeard/db migrate",
    "db:reset": "bun run --filter @servicebeard/db reset",
    "db:studio": "bun run --filter @servicebeard/db studio"
  },
  "devDependencies": {
    "@servicebeard/db": "workspace:*",
    "@servicebeard/providers": "workspace:*",
    "@servicebeard/shared": "workspace:*",
    "@types/bun": "latest",
    "@eslint/js": "^9.0.0",
    "eslint": "^9.0.0",
    "prettier": "^3.0.0",
    "typescript": "^5.6.0",
    "typescript-eslint": "^8.0.0"
  }
}
